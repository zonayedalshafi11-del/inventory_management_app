export const environment = {
  production: false,
  // Direct URL avoids proxy issues when `ng serve` is run without proxy.conf.json
  apiBaseUrl: 'http://localhost:8081/api',
  appName: 'InvenFlow',
};
